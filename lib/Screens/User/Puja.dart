import 'package:flutter/material.dart';
class Puja extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(title: Text("Puja"),centerTitle: true,) ,
      drawer: Drawer(),
      body: ListView(),
    );
  }
}
